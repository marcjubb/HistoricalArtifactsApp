<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HistoricalArtifactsApp"
        tools:targetApi="31">
        <activity android:name=".ArtifactActivity">

        </activity>
        <activity android:name=".ReviewDetailActivity">

        </activity>
        <activity android:name=".EditArtifactActivity">

        </activity>
        <activity android:name=".AdminRequestActivity">

        </activity>
        <activity android:name=".UserRequestActivity">
        </activity>
        <activity android:name=".ArtifactDetailActivity">
        </activity>
        <activity android:name=".CreateArtifactActivity">
        </activity>
        <activity android:name=".CreateUserActivity">
        </activity>
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>